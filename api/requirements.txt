fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]==2.5.0
httpx==0.25.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiofiles==23.2.1
sqlalchemy==2.0.23
alembic==1.12.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
aiofiles==23.2.1
prometheus-client==0.19.0
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0
opentelemetry-exporter-otlp==1.21.0
redis==5.0.1
python-dotenv==1.0.0

# Statistical analysis for Coherence Ratchet detection
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0

# Ed25519 signature verification
pynacl>=1.5.0

# PII scrubbing for full_traces
# Pin spaCy + model wheels together so the build is reproducible and
# nothing has to phone home at runtime. The non-root container user
# (cirislens, uid 1000) cannot write to /opt/venv, so any
# `spacy.cli.download()` invocation at request time is fatal —
# `download()` calls `sys.exit()` when its pip subprocess fails.
spacy==3.8.11
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
xx_ent_wiki_sm @ https://github.com/explosion/spacy-models/releases/download/xx_ent_wiki_sm-3.8.0/xx_ent_wiki_sm-3.8.0-py3-none-any.whl

# ONNX Runtime for the v2 NER fast path (ort + INT8). The Python
# package ships `libonnxruntime.so` which the Rust `ort` crate loads
# dynamically via `ORT_DYLIB_PATH` set in the Dockerfile. Verified
# compatible with ort 2.0.0-rc.10 (Rust crate) on the local bench.
onnxruntime==1.25.0

# CIRIS persistence engine (FSD CIRIS_PERSIST §3.5). PyO3 abi3-py311
# wheel; one wheel per target — manylinux_2_34_x86_64 +
# manylinux_2_34_aarch64 + macosx_11_0_arm64 — matches the lens
# deployment + dev matrix. Replaces the previous in-Dockerfile
# git-clone-and-maturin-compile-from-source pattern (~75 min cold
# build per architecture) with pre-built PyPI wheel install (~10 s)
# per CIRISPersist#3 close at v0.1.13. Bump this pin when persist
# ships a new release we want; provenance proven via PyPI sigstore
# attestation (PEP 740) + CIRISRegistry round-trip on the
# BuildManifest hybrid signature.
#
# v0.1.13 — initial PyPI publish (PoB substrate primitives).
# v0.1.14 — interim release.
# v0.1.15 — fixes the URL-safe base64 signature-decode mismatch that
#           caused 100% verify_invalid_signature on real Python-agent
#           traffic. Agents emit Ed25519 sigs as URL-safe base64
#           (`-`, `_`, no padding); persist ≤0.1.14 used the standard
#           alphabet decoder which truncated to 62-63 bytes. v0.1.15
#           accepts both alphabets. Diagnostic capture in
#           ./diagnostic/ proved the byte-length math.
# v0.1.16 — try-both canonical structure on verify. Agents currently
#           sign 2-field shape (`{components, trace_level}`) per the
#           lens-legacy verifier convention; the wire-format spec at
#           TRACE_WIRE_FORMAT.md §8 calls for a 9-field shape. v0.1.16
#           verifies against both and succeeds on either. CIRISAgent#710
#           tracks the agent-side migration to 9-field spec; persist's
#           fallback can drop the 2-field path on a future minor once
#           the fleet has flipped.
# v0.1.17 — diagnostic breadcrumb on the verify_unknown_key reject path
#           (per CIRISPersist#6). When lookup_public_key returns
#           Ok(None), persist now logs envelope_signer_id +
#           looked_up_id_bytes_hex + accord_public_keys_size +
#           sample of known ids. Capture with RUST_LOG=ciris_persist=warn.
# v0.1.18 — extended breadcrumb + Engine.debug_canonicalize(body) PyO3
#           method. canonical_diff at scripts/diagnostic/ pinned the
#           drift to float formatting (Python repr vs Rust serde_json/ryu).
# v0.1.19 — initial float-formatter attempt (lexical-core); INCOMPLETE
#           — still byte-diverged from Python's repr because both
#           libraries implement "shortest round-trip" with different
#           tie-breaking from CPython's Py_dg_dtoa. Skipped.
# v0.1.20 — architectural correction (closes CIRISPersist#7 for real).
#           Preserves wire-token bytes verbatim through canonicalization
#           via serde_json's arbitrary_precision feature — never decodes
#           a float to f64 in the first place, so there's nothing to
#           re-emit incorrectly. Verified byte-exact against all 3
#           YO-REJECTED captures (6/6 hash matches; both 2-field and
#           9-field forms). Lens unblocked.
# v0.2.0  — federation directory (registry-aligned). Adds V004 schema
#           with `cirislens.federation_keys` table + Engine federation
#           methods (put_public_key, lookup_public_key, ...). Schema
#           applies cleanly alongside `cirislens.accord_public_keys`.
# v0.2.1  — Engine.sign / Engine.canonicalize_envelope on the PyO3
#           surface. Backend::lookup_public_key dual-reads federation_keys
#           first then falls back to accord_public_keys — so the trace-
#           verify path picks up federation_keys writes immediately
#           with no per-version cutover dance. Legacy accord_public_keys
#           fallback retires at v0.4.0 per persist roadmap.
# v0.2.2  — separate steward identity. Engine takes optional
#           `steward_key_id` + `steward_key_path` (32-byte raw Ed25519
#           seed); exposes `engine.steward_sign(bytes) -> bytes` and
#           `engine.steward_public_key_b64()`. Distinct from the
#           keyring-managed P-256 scrub identity (signing_key_id), so
#           lens-scrub-v1 keeps signing per-row scrub envelopes (P-256)
#           while lens-steward signs federation_keys rows (Ed25519).
#           Both-or-neither construction — passing only one of
#           steward_key_id / steward_key_path raises ValueError.
#
# The lens-steward row in federation_keys is bootstrapped out-of-band
# by the bridge (one-time per deploy, parametrized SQL using
# Engine.canonicalize_envelope from a v0.2.2 install). The lens code
# below MIRRORS subsequent agent registrations into federation_keys
# alongside the existing accord_public_keys INSERT — additive during
# the v0.2.x/v0.3.x migration window; accord_public_keys writes drop
# at v0.4.0 when persist retires the dual-read fallback.
#
# v0.3.0  — wire format 2.7.9 support. Persist deterministically
#           dispatches by `trace_schema_version`:
#             - 2.7.0 → 4-field canonical (legacy fleet)
#             - 2.7.9 → 5-field canonical with per-component
#                       agent_id_hash + required parent_event_type /
#                       parent_attempt_index on LLMCallEvent
#           Per-component agent_id_hash injection at 2.7.0 is stripped
#           on the wire so the cross-shape attack surface is closed.
#           Anything else rejects with typed UnsupportedSchemaVersion.
#           Closes the spec gap CIRISAgent#712 named (binds the AV-9
#           dedup tuple structurally in the wire format), shipped with
#           the agent's release/2.7.9 cutover (PR #714 → cc41f315f).
#
#           Transitively installs ciris-verify >=1.8.6,<2, which puts
#           `ciris-build-sign` + `ciris-build-verify` on PATH via
#           console_scripts. The CI workflow's separate gh-release
#           download for ciris-build-sign retires with this bump (see
#           .github/workflows/docker-publish.yml).
#
#           Lens-side change scope for 2.7.9 traffic readiness: this
#           pin bump + the CI cleanup. Persist owns the gate; lens
#           just consumes whatever persist accepts.
#
# v0.3.1  — persist-owned cold-path PQC fill-in (closes CIRISPersist#10).
#           Built on CIRISVerify v1.9.0's PqcSigner trait +
#           MlDsa65SoftwareSigner. Engine takes optional
#           `steward_pqc_key_id` + `steward_pqc_key_path` (32B raw
#           ML-DSA-65 seed, both-or-neither). When configured, every
#           put_public_key / put_attestation / put_revocation
#           automatically fires a fire-and-forget tokio cold-path
#           after the Ed25519 hot-path commit: canonicalize, sign
#           (canonical || classical_sig) per the bound-signature
#           pattern, call attach_*_pqc_signature to populate
#           pqc_completed_at. Hot path returns in milliseconds; PQC
#           fills within seconds.
#
#           No consumer-side worker needed (architectural inversion of
#           the v0.3.0 framing where this was on lens). Same drift-
#           prevention argument as CIRISPersist#7: anything byte-stable
#           across federation consumers belongs in the substrate.
#
#           Lens-side change scope: this pin bump + two new env vars
#           wired through to cp.Engine (CIRISLENS_STEWARD_PQC_KEY_ID
#           + CIRISLENS_STEWARD_PQC_KEY_PATH). Bridge mounts the
#           ML-DSA-65 seed alongside the existing Ed25519 seed.
#           Backwards-compatible: v0.3.0 callers unchanged; new params
#           are additive.
#
# v0.3.2  — closes the two outstanding architectural follow-ups in
#           one tag:
#
#           CIRISPersist#11 — Engine.run_pqc_sweep() + auto-fire on
#           Engine construction. Walks federation_* rows where
#           pqc_completed_at IS NULL, re-canonicalizes via persist's
#           own PythonJsonDumpsCanonicalizer, signs (canonical ||
#           classical_sig) with the configured steward_pqc identity,
#           calls attach_*_pqc_signature. Auto-fire on init drains
#           any pre-existing hybrid-pending backlog passively the
#           next time persist boots — exactly what V004's Phase 2
#           "pre-flip rows get walked through the upgrade pipeline"
#           contract presupposes. Recovers from cold-path failures
#           too (transient sign errors, mid-write process restarts).
#
#           CIRISPersist#9 — V005 read-only role + public column
#           contract. `cirislens_reader` PostgreSQL role provisioned
#           at migration time with SELECT-only privileges across
#           cirislens.* tables. PUBLIC_SCHEMA_CONTRACT.md documents
#           which columns downstream consumers can rely on, with
#           stability tiers (stable / stable-ro / internal). Lens-
#           side wiring of CIRISLENS_READ_DSN for analytical paths
#           ships separately when we're ready to migrate trace_context
#           view + science scripts to the read-only connection;
#           v0.3.2 just makes the surface available.
#
#           Lens-side change scope for the cold-path sweep: pin bump
#           only. Auto-fire on init drains the 654 hybrid-pending
#           rows authored under v0.3.0 / v0.3.1's-without-PQC-mount
#           the next time bridge redeploys. tracing::info! summary
#           line in lens logs confirms completion. No code change,
#           no env var, no bridge prereq — the existing PQC seed
#           mount that landed for v0.3.1 carries forward.
#
# v0.3.3  — closes CIRISPersist#12 (LlmCallSummary missing fields +
#           decompose substitution + presence-only validation gap
#           that bridge's v0.3.0 deploy validation passed despite the
#           regression). Three coordinated changes:
#             1. parent_event_type + parent_attempt_index added to
#                the LlmCallSummary serde struct (REQUIRED at 2.7.9).
#             2. decompose.rs reads from the struct fields instead of
#                substituting component.event_type (which was always
#                literally "LLM_CALL" for an LLM_CALL component —
#                defeated AV-9 parent-linkage signal).
#             3. Parse-time validation: 2.7.9 LLM_CALL events without
#                both fields reject with typed UnsupportedSchemaVersion;
#                the v0.3.0 "required at 2.7.9" gate is now value-
#                correct, not just presence-correct.
#
#           Paired with CIRISAgent e714ff3c4 (#715) which fixes the
#           agent-side wire-emission of those fields. **Both must
#           deploy together** — persist 0.3.3's parse-time rejection
#           would 422 in-flight 2.7.9 LLM_CALL traffic during the
#           rollout if the agent fix isn't already live. Bridge
#           coordinates the bump.
#
#           Lens-side change scope: this pin bump. No code change,
#           no env var. Post-deploy verification:
#             SELECT parent_event_type, COUNT(*)
#             FROM cirislens.trace_llm_calls
#             WHERE schema_version = '2.7.9'
#               AND ts > <deploy_ts>
#             GROUP BY 1;
#           Should show upstream-step taxonomy (ASPDMA_RESULT,
#           IDMA_RESULT, CONSCIENCE_RESULT, ...) instead of 100%
#           LLM_CALL. CIRISLens#5 closes once that query confirms.
#
# v0.3.4  — closes CIRISPersist#13 — adds the deployment_profile block
#           (6 fields: agent_role, agent_template, deployment_domain,
#           deployment_type, deployment_region, deployment_trust_mode)
#           to every CompleteTrace envelope at trace_schema_version
#           2.7.9. Strict-parse: 2.7.9 envelopes without the block
#           reject with MissingField. Cross-shape rule preserved:
#           2.7.0 envelopes carrying the block parse cleanly but the
#           field does NOT enter 2.7.0 canonical bytes (mirrors the
#           per-component agent_id_hash pattern from v0.3.3).
#
#           10-key 2.7.9 outer canonical (was 9). deployment_profile
#           sorts between components and started_at alphabetically.
#
#           Paired with CIRISAgent 431b0e0ae (#718) which adds the
#           wire-side emission. Same coordinated-deploy pattern as
#           0.3.3: both must land together — persist 0.3.4 hard-rejects
#           2.7.9 traffic without the block, so any agent emitting
#           2.7.9 must also be on 431b0e0ae or 422-rejected.
#
#           Lens-side change: pin bump only. Post-deploy validation
#           query verifies deployment_profile breakdown lights up
#           across the 2.7.9 fleet (per bridge runbook).
#
# v0.3.5  — closes three CIRISLens#8 asks in one release:
#
#   ASK 1: Engine.delete_traces_for_agent(agent_id_hash, include_
#          federation_key=False) — DSAR primitive. Deletes persist-
#          owned post-cutover data (trace_events + trace_llm_calls;
#          optionally federation_keys + attestations + revocations
#          when include_federation_key=True). Lens DSAR handler folds
#          onto this for persist-owned tables; lens-owned legacy
#          (accord_traces, accord_traces_mock) and lens-derived
#          (coherence_ratchet_alerts) keep their direct DELETE.
#
#          Scope shift: persist's API is per-agent_id_hash, broader
#          than lens's pre-fold per-(agent_id_hash, signature_key_id)
#          scope. That's correct for GDPR Article 17 (data subject
#          is the person, not the key); pre-cutover accord_traces
#          deletion retains the per-key scope because that's the
#          contract those rows were stored under.
#
#   ASK 2: v0.4.0 timing — accord_public_keys dual-read retires;
#          lens-side direct INSERT into accord_public_keys can retire
#          the same release. Coordinate at v0.4.0 cut.
#
#   ASK 3: Engine.fetch_trace_events_page(after_event_id, limit,
#          agent_id_hash) — page-cursor read primitive. Useful for
#          future analytical migrations; not used in this lens
#          release. Available for the next analytical-path fold.
#
#          Lens-side change scope: this pin bump + DSAR handler
#          fold (~30 lines in accord_api.py).
#
#          Update: lens v99359e8 attempted the DSAR fold using the
#          v0.3.5 per-agent API. Reverted at fbbd844 because the
#          per-agent scope broadens beyond the lens DSAR contract
#          (per-(agent_id_hash, signature_key_id); the signature_key_id
#          filter is the AUTHORIZATION scope, not just identity).
#          Filed CIRISPersist#15 asking for the signature_key_id
#          parameter; closed in v0.3.6.
#
# v0.3.6  — closes two issues:
#
#   #14 — Engine.verify_hybrid: day-1 hybrid Ed25519+ML-DSA-65 verify
#         primitive. Unblocks CIRISEdge OQ-11 (PQC verify timeline).
#         Lens doesn't consume this directly today (verify happens
#         inside Engine.receive_and_persist for trace ingest); but
#         when CIRISEdge lands and absorbs the wire-side verify,
#         hybrid posture is available out of the box.
#
#   #15 — Engine.delete_traces_for_agent now requires signature_key_id.
#         BREAKING vs v0.3.5 (no escape hatch): the per-agent-only
#         API in v0.3.5 was a security mistake — broadens the DSAR
#         scope beyond per-key authorization, allowing one valid key
#         to delete traces signed by other keys claiming the same
#         logical agent. v0.3.6 makes signature_key_id REQUIRED.
#
#         Lens-side change scope: pin bump + DSAR handler fold lands
#         cleanly. The persist-owned half (trace_events +
#         trace_llm_calls) routes through Engine with the per-key
#         filter intact; lens-owned legacy (accord_traces,
#         accord_traces_mock) and lens-derived (coherence_ratchet_alerts)
#         keep their direct DELETE on db_pool. include_federation_key
#         stays False so the agent's signing key survives the DSAR
#         (matches pre-fold lens behaviour: agent can register fresh
#         consent + start a new corpus).
#
# v0.4.0  — federation substrate cut. Closes CIRISPersist#16 +
#           CIRISLens#8 ASK 2; adds AV-40 + AV-41 to persist threat model.
#
#   What ships:
#     ─ edge_outbound_queue durable substrate for
#       CIRISEdge::send_durable() (closes CIRISEdge OQ-09 — durable
#       channel for messages that must eventually land:
#       BuildManifestPublication, DSARRequest/Response,
#       AttestationGossip, PublicKeyRegistration).
#     ─ accord_public_keys dual-read fallback RETIRED. Verify path
#       now reads federation_keys exclusively. Pre-cutover keys are
#       already in federation_keys (bridge bootstrap + lens migrator
#       backfilled at v0.2.x; 660 rows confirmed live).
#     ─ Full verify surface exposed via 5 new Engine methods —
#       unblocks agent runtime cutover via lens-core.
#     ─ Threat model AV-40 + AV-41 (durable-queue-specific vectors).
#
#   Lens-side change scope for THIS commit: pin bump only.
#
#   Deferred to a focused follow-up PR (CIRISLens#8 ASK 2 closure):
#     ─ Drop direct INSERT into accord_public_keys in
#       register_public_key (federation_mirror via
#       Engine.put_public_key has been the load-bearing write since
#       v0.2.x; the direct INSERT is now a write to a no-longer-read
#       table).
#     ─ Switch list_public_keys / verify-cache reads to
#       federation_keys (paired with the INSERT drop so analytical
#       reads don't go stale).
#
#   Production verification before this pin bump:
#     2.7.9 traffic happy — 854 events / 31 traces in last hour;
#     canonical parent_event_type taxonomy (no LLM_CALL substitution);
#     deployment_profile populated; zero 422s in logs.
#     Generic-tier agent_id_hash='unknown' tracking on
#     CIRISAgent#716 (open upstream; not blocking).
#
# v0.4.1  — Rust-side verify primitives + curated prelude (CIRISEdge ask).
#           Non-breaking; new public Rust API surface only. Adds
#           verify::verify_hybrid_via_directory free function backing
#           the PyO3 path (CIRISPersist#7 single-source-of-truth).
#           No lens-side change required — PyO3 surface unchanged.
#
# v0.4.2  — Rust-public StewardSigner (CIRISPersist#17). CIRISLensCore
#           (rlib path, never PyO3) needs to sign detection events via
#           persist's steward identity; v0.4.2 lifts construction +
#           sign primitives to a Rust-public struct. PyO3 Engine refactored
#           to back onto it. PyO3 surface unchanged from lens's POV.
#
# v0.4.3  — lens-derived schemas + 2.7.legacy restoration
#           (CIRISPersist#18 + #21). Two issues, one release:
#
#   #18 — cirislens_derived.{detection_events, calibration_bundles}.
#         V008 migration adds the schemas with hybrid-sig CHECK
#         constraints (Ed25519=64 bytes, ML-DSA-65=3309 bytes per FIPS
#         204 final), partial-unique index for atomic is_current flip
#         on calibration bundles. New Engine surface (5 methods on
#         DerivedSchema trait): put_detection_event /
#         get_detection_events / put_calibration_bundle /
#         get_current_calibration_bundle / get_calibration_bundle_by_version.
#         Engine PyO3 verifies hybrid sigs via verify_hybrid_via_directory
#         under HybridPolicy::Strict before backend write — both sigs
#         must verify, no fallback.
#         Lens consumption: deferred — cirislens_derived is the LensCore
#         landing zone for projection-v1 publications. Pin bump
#         alone does not start writing; LensCore work picks this up.
#
#   #21 — 2.7.legacy restoration. v0.4.0 dropped the legacy 2-field
#         canonical on a fleet-migration framing that doesn't fit the
#         decentralized model. v0.4.3 re-adds "2.7.legacy" to
#         SUPPORTED_VERSIONS under the same observable-traffic sunset
#         rule already specified for "2.7.0" (drop once
#         federation_canonical_match_total{wire="2.7.legacy"} stays at
#         zero through the soak window). Deterministic dispatch — a
#         trace whose canonical bytes match the 2-field shape verifies
#         via canonical_payload_value_legacy; not a try-list, not a
#         flag, not operator-opt-in. Federation-uniform behavior.
#         Lens-side benefit: pre-2.7.8.9 federation peers (any agent
#         that pre-dates trace_schema_version stamping at commit
#         431b0e0ae) verify via Engine.receive_and_persist again.
#
# v0.4.4  — CI hygiene patch (no behavioral change vs v0.4.3).
#           Fixes a stale test asserting SUPPORTED_VERSIONS=["2.7.0",
#           "2.7.9"] and one cargo fmt drift introduced during #21
#           work. Adds pre-commit + pre-push hooks to prevent
#           regression class. Functionality identical to v0.4.3 — the
#           bump is for the green CI tag the publish flow requires.
#
# v0.4.5  — CIRISVerify dep bump v1.9.0 → v1.13.2 (CIRISPersist#20).
#           Pure dep-only; no public-API or behavior change in any
#           persist code path lens consumes. Jumped past the issue's
#           v1.10.1 target because v1.10.0..v1.13.2 is all CLI /
#           RegistryClient / verify_tree work — none touches
#           ciris-keyring, HybridVerifier, or ciris-crypto's primitive
#           surface persist depends on. Lands persist on the current
#           verify line in one move.
#
# v0.4.6  — legacy attempt_index gate + decompose error reclass +
#           field-name surfacing (CIRISPersist#22 — closes
#           CIRISLens#9 absorption gap discovered post-2434273
#           lens-side stamp rewrite).
#
#   The fix lives in three places, all in CIRISPersist:
#
#   - decompose.rs:82 — schema-version-gated attempt_index sourcing.
#         Pre-2.7.8 emitters never populate `data.attempt_index`
#         (CIRISAgent v2.7.6-stable's accord-metrics adapter has zero
#         references to it). 2.7.9 strict; 2.7.0/2.7.legacy fall back
#         to 0 ONLY for the absence case — malformed values (negative,
#         wrong type, out of range) still error. Same shape as the
#         existing parent_event_type/parent_attempt_index gate
#         (CIRISPersist#12, v0.3.3).
#         Dedup-collapse cost: retries on a 2.7.legacy trace dedup to
#         attempt_index=0 with the original. Acceptable for legacy
#         backfill — alternative was dropping pre-2.7.8.9 traces
#         entirely. Sunsets under the same observable-traffic rule
#         persist applies to its 2.7.legacy arm itself
#         (federation_canonical_match_total{wire="2.7.legacy"} zero
#         through 7-day soak; src/schema/version.rs:36-39).
#
#   - ingest.rs:229 — typed Schema/Store split in decompose's
#         map_err. Was: `crate::store::decompose(trace).map_err(IngestError::Store)?`
#         which indiscriminately wrapped typed `Error::Schema(_)` from
#         decompose as Store, surfacing at FFI as PyRuntimeError →
#         lens 503 → "Retry-After: 5" → agent retries forever on a
#         deterministic 4xx. Bridge observed Scout1 retrying ~3/min
#         for two days on the now-relaxed attempt_index gate.
#         Now: typed match, `store::Error::Schema(s) → IngestError::Schema(s)`,
#         everything else → Store. Schema → 422 (deterministic reject;
#         agent stops retrying). Backend write errors at the two
#         insert_*_batch callsites stay on Store (they legitimately
#         return RuntimeError → 503 + Retry-After).
#
#   - IngestError::detail() + schema::Error::detail() — non-breaking
#         field-name surfacing. PyO3 emits Python exception args as
#         (kind, detail) tuple when detail is present, (kind,) only
#         otherwise. Lens reads e.args[1] for the field name without
#         source-diving persist (AV-15 sanitization preserved — kind
#         remains the stable wire token; detail is structured field
#         identifier, not free-form attacker text). NB lens
#         _delegate_to_persist still reads `str(e)` for its 422/503
#         mapping, which is unchanged: `str()` of single-arg exception
#         == kind; `str()` of two-arg exception == "('kind', 'detail')"
#         repr — both fall through to the 422 default branch correctly.
#         Future lens-side cleanup (separate PR): read e.args directly
#         to format a clean "schema_missing_field: attempt_index"
#         HTTPException detail.
#
#   Why both the lens stamp rewrite (commit 2434273, CIRISLens#9) AND
#   this persist bump are required: persist's verify dispatch is by
#   stamped trace_schema_version (deterministic, NOT shape-sniffing).
#   Pre-2.7.8.9 emitters stamp "2.7.0" + sign 2-field canonical, so
#   without lens flipping the stamp to "2.7.legacy" persist routes them
#   to the 9-field canonicalizer and signature verify fails. The
#   v0.4.6 attempt_index relaxation only fires on the 2.7.legacy arm,
#   which the lens shim is what gets traces routed into.
#
# v0.4.7  — threat-model documentation only (no code change). AV-35
#           mitigation language tightened (verify-bound-to-arm-canonical,
#           not signed-routing-input); AV-42 added covering the legacy
#           attempt_index dedup-collapse v0.4.6 shipped. §9 posture
#           summary updated v0.3.6 → v0.4.6. Pure docs; functionality
#           identical to v0.4.6.
#
# v0.5.0  — federation read primitives §A/B/F/E (CIRISPersist#23 —
#           closes the lens-bleeding read/write split CIRISLens#10
#           surfaced + the /coherence-ratchet/stats 500 + api/scoring.py
#           raw-SQL violation).
#
#   The architectural invariant: no consumer reads persist's tables
#   directly. Every read goes through persist's typed PyO3 surface
#   (same shape v0.4.1 established for verify primitives — Rust-public
#   free function + PyO3 wrapper, single source of truth). This pin
#   makes the load-bearing four sections of #23 available; lens-side
#   migration of ~50 raw-SQL SELECTs follows in subsequent commits as
#   each read path is rewritten against the new primitives.
#
#   This is also the validation gate for v0.5.1 — persist needs lens
#   to exercise v0.5.0 in production before shipping the deferred
#   sections (C/D/G/H/I + final cirislens_reader carve-out retirement).
#
#   12 new PyO3 methods. Wire format: JSON strings in/out for complex
#   types (TraceFilter, TraceCursor, TraceSummary, TraceListPage,
#   TraceDetail, TimeWindow, DivergenceRow, OverrideRateRow,
#   ScoringFactorAggregate, AuditChainAggregate), primitives as direct
#   args (trace_id, agent_id_hash, deployment_domain, metric, limit).
#   Same idiom as put_attestation / put_detection_event — lens calls
#   json.dumps before passing in, json.loads on receiving back.
#
#   §A — trace listing (drives /repository/traces, CIRISLens#10):
#         list_trace_summaries(filter_json, cursor_json=None, limit=100)
#             → TraceListPage{"items":[TraceSummary],"next_cursor":...}
#         get_trace_summary(trace_id) → Optional[TraceSummary]
#         JSONB-extracting GROUP BY with FILTER aggregation in one DB
#         pass; cursor pagination via (started_at, trace_id) tuple.
#
#   §B — trace detail (drives /repository/traces/{trace_id}):
#         get_trace_detail(trace_id) → Optional[TraceDetail]
#         3 round-trips composing summary + components (chronological)
#         + LLM calls + envelope refs.
#
#   §F — Coherence Ratchet inputs (drives /coherence-ratchet/stats —
#         currently 500'ing in prod against the dead accord_traces):
#         cross_agent_divergence(deployment_domain, window_json, metric)
#             → JSON array of DivergenceRow. metric ∈ {csdma_plausibility,
#             dsdma_domain_alignment, idma_k_eff, idma_correlation_risk,
#             conscience_override_rate}.
#         temporal_drift(agent_id_hash, baseline_json, comparison_json)
#             → JSON array of TemporalDriftRow (Welch z-score).
#         hash_chain_gaps(agent_id_hash, window_json)
#             → JSON array of HashChainGap (LAG over audit_sequence_number).
#         conscience_override_rates(deployment_domain, window_json)
#             → JSON array of OverrideRateRow (population-weighted
#             domain average).
#
#   §E — scoring factor aggregates (replaces api/scoring.py raw SQL):
#         aggregate_scoring_factors(agent_id_hash, window_json,
#                                   baseline_window_json=None)
#             → ScoringFactorAggregate (Capacity Score C/I_int/R/I_inc/S
#             inputs in one round-trip).
#         aggregate_scoring_factors_batch(agent_id_hashes_json,
#                                         window_json,
#                                         baseline_window_json=None)
#             → list[ScoringFactorAggregate] — one round-trip for a
#             fleet sweep, not N.
#         Granular: count_traces / count_overrides /
#                   count_identity_changes / aggregate_audit_chain
#                   for ad-hoc narrower questions.
#
#   AV-43 added to docs/THREAT_MODEL.md — read-side adversary inference
#   attack. Aggregates return computed statistics, not per-trace content;
#   sample_count/trace_count surface explicitly for caller-side
#   k-anonymity gates. Posture summary §9 bumped v0.4.6 → v0.5.0.
#
#   Deferred to v0.5.1 (after lens validates v0.5.0):
#     §C — task-grouped listing (lens groups in JS from §A flat list
#          as v0.5.0 workaround; UI grouping is a website concern).
#     §D — LLM call surface (trace_llm_calls primitives).
#     §G — corpus shape (operator dashboards).
#     §H — scrub stats (privacy observability).
#     §I — federation observability bulk (monitoring dashboards).
#     Plus full cirislens_reader carve-out retirement (gated on §D).
#
#   FSD/V0_5_0_FEDERATION_READ_PRIMITIVES.md documents the shape;
#   THREAT_MODEL.md AV-43 documents the new invariant. 19 integration
#   tests passing against real Postgres in the persist repo.
#
# v0.5.1  — P0 hotfix: aggregate_scoring_factors empty-window panic
#           (CIRISPersist#24).
#
#   Closes prod wedge 2026-05-11 15:09-15:59 UTC. Triggered by lens
#   probing the §E baseline path against an agent with zero traces
#   in the baseline window: persist's main SELECT ran without GROUP
#   BY, the empty input CTE made `SUM(CASE WHEN ...)` return NULL,
#   `Row::get::<_, i64>` panicked, and (because v0.5.0's release
#   profile had `panic = "abort"`) PyO3's catch-unwind trampoline
#   couldn't fire — every uvicorn worker hit SIGABRT in parallel
#   from concurrent baseline calls and the process wedged including
#   /health.
#
#   Fix is belt-and-braces at BOTH layers:
#   - SQL: COALESCE(SUM(...), 0) on conscience_overrides /
#          audit_chain_total / audit_signed_total /
#          unsafe_action_count.
#   - Rust: try_get::<_, Option<i64>>(col)?.unwrap_or(0) so a future
#          SQL edit that drops a COALESCE surfaces as HTTP 500, not
#          a Rust panic + SIGABRT.
#
#   Two regression tests added; the first one failed with the exact
#   prod crash signature when COALESCE reverted, then passed
#   cleanly with the fix.
#
#   Lens-side mitigation in commit 80a20e6 was conservative — the
#   §E pass-through endpoints accept baseline_hours but never
#   constructed pathological windows on their own. The trigger was
#   organic probing; ANY caller hitting the §E baseline path on a
#   sparse-baseline agent could have repro'd. v0.5.1 closes it from
#   both ends.
#
# v0.5.2  — bump CIRISVerify deps v1.13.2 → v2.0.0 (fixes ml_dsa CI
#           break, behavior-neutral for persist consumers).
#
#   Local v0.5.1 builds passed against the cached Cargo.lock but CI
#   fresh-resolved and picked up a breaking ml-dsa transitive minor
#   that removed `KeyGen` + `MlDsa65::from_seed`. CIRISVerify v2.0.0
#   pins ml-dsa = 0.1.0-rc.9 exact, closing the drift hazard.
#
#   Available-but-not-activated by v0.5.2:
#   - aes-gcm / kdf / hmac / random on ciris-crypto (v0.6.0 prereq
#     for #19 federated SecretsService)
#   - symmetric-derivation on ciris-keyring (v0.6.0 prereq)
#
#   Crypto-through-ciris-crypto invariant (FSD §7.5a) unchanged —
#   persist still takes ZERO direct deps on primitive crates.
#   v0.6.0 flips features on existing deps; no new crates land.
#
# v0.5.3  — three-layer panic isolation (CIRISPersist#25 + #26 + #27)
#           + verify deps v2.0.0 → v2.0.2.
#
#   Hardening track on top of v0.5.1's point-fix. Three orthogonal
#   layers of defense against the entire SUM-NULL → Row::get →
#   SIGABRT failure class:
#
#   - #25 (SQL → Rust):   panic = "abort" → "unwind" in release.
#         PyO3's catch_unwind trampoline now fires; Rust panics
#         become PanicException not SIGABRT.
#         SECURITY_AUDIT_v0.1.2.md §4.2's abort rationale was right
#         for v0.1.x standalone-bin but doesn't survive
#         v0.5.x cdylib-in-uvicorn — reframed in THREAT_MODEL.md
#         §3.13 + AV-44.
#   - #26 (Rust):         PgRowExt::safe_get trait — try_get with
#         typed Backend error mapping swept across ~80 sites in the
#         v0.5.0 ReadEngine impl + decode helpers. NULL surfaces as
#         HTTP 500 with column name, not Rust panic. Pre-v0.5.0
#         sites tracked in CIRISPersist#28 for v0.5.4 sweep
#         completion.
#   - #27 (FFI → Python): pyo3::create_exception! LensQueryError +
#         catch_panic(AssertUnwindSafe(...)) wrapping all 13 v0.5.0
#         ReadEngine PyO3 methods. Caught panics convert to
#         LensQueryError(Exception); uvicorn's "except Exception"
#         catches as clean 500 instead of escaping as
#         PanicException (which inherits from BaseException and
#         would still tear the worker).
#
#   Verify deps v2.0.1 → v2.0.2 closes the ml-dsa → pkcs8 caret-
#   range hazard (CIRISVerify#18); v2.0.2 pins pkcs8 exact.
#
#   Threat model: AV-44 added; §3.13 new; §9 posture summary bumped
#   v0.5.0 → v0.5.3; 17 vectors closed across v0.2.0..v0.5.3.
#
#   What v0.5.3 means for lens: every future Rust bug in the
#   §A/§B/§E/§F PyO3 surface surfaces as a typed
#   LensQueryError → HTTP 500, not a worker-pool wipe. The matrix
#   we validated in 80a20e6 (12-of-12 baseline-free probes ✅,
#   3-of-3 baseline probes broken) re-runs cleanly against v0.5.3.
#   Pre-v0.5.0 surface (federation directory, verify primitives)
#   still on the old panic discipline — CIRISPersist#28 sweep + #29
#   Python-side regression test land in v0.5.4.
#
# v0.5.4  — finishes the panic-isolation track v0.5.3 started.
#           CIRISPersist#28 (full sweep) + #29 (Python regression
#           gate). 1da2503 → 1da2503-style probe matrix re-runs
#           unchanged; net effect is invariant durability, not
#           observable behavior change for v0.5.0 callers.
#
#   #28 part 1 — PgRowExt extension + full postgres sweep.
#         PgRowExt generalized: safe_get<T, I> on RowIndex (column
#         name OR ordinal); safe_get_with<T, I, E, F> for non-
#         ReadEngine layers that need to route NULL into their own
#         Error::Backend variant. ~75 additional bare Row::get
#         sites swept across src/store/postgres.rs — federation
#         directory decoders (pg_row_to_key_record / _attestation /
#         _revocation lifted infallible → Result), pg_row_to_event_row,
#         pg_row_to_outbound_row, list_hybrid_pending_*,
#         lookup_public_key, sample_public_keys,
#         delete_traces_for_agent, enqueue_outbound,
#         mark_transport_failed, count_traces, count_overrides,
#         count_identity_changes, aggregate_audit_chain. SQLite
#         exempt by construction (rusqlite::Row::get returns Result
#         on NULL, not the tokio_postgres panic class).
#         CI gate: scripts/hooks/pre-commit rejects bare `row.get(`
#         and `.get::<` patterns in src/store/postgres.rs at commit
#         time — regression class can't sneak back in.
#
#   #28 part 2 — FFI catch_panic sweep.
#         v0.5.3 wrapped 13 v0.5.0 ReadEngine PyO3 methods. v0.5.4
#         wraps the remaining 53 pre-v0.5.0 entry points (federation
#         directory writers, outbound queue ops, derived-schema
#         CRUD, verify primitives, canonicalization helpers,
#         steward signing, debug methods). Every PyO3 method on
#         PyEngine (~70 entry points) now routes panic through
#         catch_panic, converting PanicException (BaseException)
#         into LensQueryError (Exception). Applied via deterministic
#         brace-depth scan; no proc-macro infra introduced.
#
#   #29 — Python regression test gate.
#         New `test-panic` feature flag in Cargo.toml. With it on,
#         a module-level #[pyfunction] _test_inject_panic bypasses
#         Engine construction (no postgres/keyring needed) and
#         panics inside catch_panic. Release wheels don't compile
#         it in. tests/python/test_catch_panic.py (5/5 passing in
#         persist CI): LensQueryError exported + subclasses
#         Exception; panic surfaces as LensQueryError, message
#         preserved; etc. This is the regression-test stand-in
#         that v0.5.3 lacked — the catch_panic invariant is now
#         empirically gated on every persist CI run.
#
#   Lens consumption: optional but recommended — handlers may
#   now `from ciris_persist import LensQueryError` and catch it
#   explicitly to log panic-converted errors with the column-name
#   context PgRowExt surfaces. Until that catch lands, our existing
#   `except ValueError → 422` / `except RuntimeError → 503` arms
#   in _delegate_to_persist do NOT match LensQueryError
#   (it inherits directly from Exception, not from RuntimeError),
#   so it propagates to FastAPI's default Exception handler →
#   uvicorn's clean 500. Functionally fine; tighter routing comes
#   in a follow-up commit.
#
# v0.5.5  — federation read primitives §C/D/G/H/I (closes CIRISPersist#23
#           remainder; v0.5.0 shipped §A/B/F/E, this lands the deferred
#           batch). 5 additive primitives, zero schema changes, zero
#           breaking edits. Every new PyO3 entry point wrapped in
#           catch_panic from the start per the v0.5.4 discipline.
#
#   §C — list_tasks: task-grouped variant of list_trace_summaries.
#        TaskClass canonical derivation server-side
#        (qa_eval / discord / real_user_* / wakeup_ritual / other)
#        from task_id prefix → uniform across federation peers.
#        initial_observation extracted from earliest THOUGHT_START's
#        task_description. Cursor on (earliest_at, task_id),
#        newest-first; trace ordering within task is thought_depth
#        ASC. Replaces lens-side JS task-grouping mention in the
#        v0.5.0 deferral block above; consumers can adopt incrementally.
#
#   §D — list_llm_calls + aggregate_llm_costs: cursor-paged listing
#        over trace_llm_calls with agent / model / status / trace
#        filters (agent filter forces JOIN to trace_events). Cost
#        rollup by_model / by_agent / by_domain + totals; every SUM
#        COALESCE'd to 0 proactively per the CIRISPersist#24
#        hygiene now in pre-commit.
#
#   §G — corpus_shape: 6 breakdowns per window — task_class,
#        qa_language, qa_question_num, agent_name, agent_version
#        (= agent_template), primary_model, deployment_region.
#        primary_model is per-trace most-frequent LLM call model.
#        stationarity_z_score reserved for a future baseline-window
#        API extension (not in v0.5.5).
#
#   §H — aggregate_scrub_stats: envelopes_scrubbed + by_trace_level
#        populate today; fields_scrubbed_total + by_entity_type
#        wired but gated on v0.6.0's post-ingest classification
#        pipeline (CIRISPersist#19). Shape locked now so consumers
#        don't churn when the pipeline lands.
#
#   §I — list_federation_keys / list_attestations / list_revocations:
#        bulk primitives over cirislens.federation_* tables. Filters
#        include revoked (EXISTS check), pqc_completed (IS NOT NULL),
#        per-key/attestation/revocation identity refs. Items reuse
#        crate::federation types — no duplicate schemas.
#
#   Test coverage: 17 new integration tests (5 §C + 4 §D + 3 §G +
#   2 §H + 3 §I). 222 lib tests pass (was 205 at v0.5.4).
#
#   Memory + SQLite backends return NotImplemented per existing
#   convention; postgres is the production path.
#
#   Lens-side adoption follows in subsequent commits — this pin
#   alone makes the primitives available; consumer rewrites
#   (§C task-grouped on the explore page, §D for cost dashboards,
#   etc.) land per-feature as the algorithm-level refactors below
#   complete.
#
# v0.5.6  — §I test-fixture fixes (pagination contract + hex shape).
#           v0.5.5's PG-gated CI surfaced two §I test bugs (cursor
#           emptiness assertion on exact-fill page; 3-char hex on
#           original_content_hash). Zero implementation changes;
#           the §C/D/G/H/I Rust code is the v0.5.5 code unchanged.
#           publish-pypi was skipped per manifest-integrity discipline
#           — no v0.5.5 or v0.5.6 wheel ever reached PyPI.
#
# v0.5.7  — third §I test-fixture iteration (revocation_id UUID
#           shape). put_revocation casts $1::uuid; the test passed
#           a hex-timestamp token that the tokio_postgres serializer
#           rejected at parameter bind. Replaced with uuid::Uuid::new_v4()
#           to match the rest of the test suite. Same manifest-
#           integrity skip pattern; no v0.5.7 wheel on PyPI.
#
# v0.5.8  — first PyPI wheel of the §C/D/G/H/I batch. Carries the
#           REAL underlying bug v0.5.6/v0.5.7's fixture iterations
#           were masking: put_revocation + put_attestation rejected
#           String-bound IDs whenever tokio-postgres' prepared-
#           statement type inference resolved $1::uuid to expect a
#           uuid::Uuid value rather than text. Latent since v0.3.x;
#           no prior end-to-end PG test exercised the put paths
#           (only SELECT-side attach_*_pqc_signature in production).
#           §I round-trip test was the first to hit it.
#
#   Fix: parse String → uuid::Uuid::parse_str at the persist
#   boundary; bind the Uuid value directly. with-uuid-1 feature on
#   tokio-postgres already provides ToSql; the &String → $N::uuid
#   cast path is retired for put_revocation + put_attestation. Other
#   $N::uuid sites (attach_*_pqc_signature, outbound queue ops)
#   unchanged — those are SELECT paths that work in prod; will
#   revisit if tests surface them. Invalid UUIDs now surface as
#   Error::InvalidArgument (was opaque Error::Backend serialization
#   error) — strictly better for operators.
#
#   Net for lens: pin jumps 0.5.4 → 0.5.8 in one step. CIRISPersist#23
#   closes on this push. Only CIRISPersist#19 (v0.6.0 post-ingest
#   pipeline) remains open in the v0.5.x → v0.6.x sequence.
#
# v0.5.8 → v0.9.1: lens-API skipped the v0.6 / v0.7 / v0.8 / v0.9
# minor lines during the lens-core cutover work — held the pin stable
# while the gift package shipped. ciris-lens-core==0.1.1 forces this
# bump because its `ciris-persist>=0.9.1,<1` constraint conflicts with
# our v0.5.8 pin. v0.6.0 / v0.6.1 partial-closed CIRISPersist#19
# (post-ingest pipeline + scrub/extract absorption); v0.7.0 added the
# CIRISNodeCore federation-consensus substrate (#30); v0.7.5 shipped
# the pipeline orchestrator + PipelineEnvelope wire types (#33);
# v0.8.x / v0.9.0 are intermediate releases; v0.9.1 is the omnibus
# 11-feature wheel that closed #41.
ciris-persist==0.9.1

# CIRISLensCore — Rust science layer for the federation. v0.1.1 is the
# first PyPI-published release; cuts over from the in-tree
# cirislens-core/ Cargo crate (compiled by the rust-builder stage in
# api/Dockerfile pre-cutover) to a published wheel.
#
# Contract: 4-function PyO3 surface
#   process_trace_batch(engine, events, batch_timestamp, ...)
#   scrub_trace(trace_json, level)
#   scrub_traces_batch(traces_json, level)
#   ner_is_configured()
#
# "Engine-as-parameter; never holds keys" — lens-core takes the persist
# Engine as an arg; persist owns signing + persistence + federation
# directory. Lens-core never holds private key material; survives the
# PoB §3.1 fold-into-agent unchanged.
#
# Wheels: linux-x86_64, linux-aarch64, darwin-aarch64 (abi3-py311).
# Federation chain at handoff: persist v0.9.1 / edge v0.1.4 /
# lens-core v0.1.1. See FSD/LENS_CORE_ADOPTION_GIFT_PACKAGE.md for the
# full handoff inventory.
ciris-lens-core==0.1.1